/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: {
    unoptimized: true,
  },
  // If you have other configurations, they will be preserved.
  // For example:
  // reactStrictMode: true,
};

export default nextConfig;
