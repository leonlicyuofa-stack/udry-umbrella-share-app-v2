// DO NOT EDIT THIS FILE!
// This file is generated by the Capacitor CLI.
// It is a gitignored file that will be recreated on every build.
apply plugin: 'com.android.application'

// Defines a function to get a property from various sources.
def getExtOrIntegerDefault(propName, defaultValue) {
    return rootProject.hasProperty(propName) ? rootProject.ext.get(propName).toInteger() : defaultValue
}

android {
    namespace "com.udry.app"
    compileSdk getExtOrIntegerDefault('compileSdkVersion', 34)
    defaultConfig {
        applicationId "com.udry.app"
        minSdkVersion getExtOrIntegerDefault('minSdkVersion', 22)
        targetSdkVersion getExtOrIntegerDefault('targetSdkVersion', 34)
        versionCode 1
        versionName "1.0"
        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"
    }

    // This signingConfigs block is the crucial addition.
    // It reads the keystore details and passwords from the environment variables.
    signingConfigs {
        release {
            if (System.getenv("UDRY_KEYSTORE_PATH") != null) {
                storeFile file(System.getenv("UDRY_KEYSTORE_PATH"))
                storePassword System.getenv("UDRY_STORE_PASSWORD")
                keyAlias System.getenv("UDRY_KEY_ALIAS")
                keyPassword System.getenv("UDRY_KEY_PASSWORD")
            }
        }
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
            // This line tells the release build to use the signing configuration defined above.
            signingConfig signingConfigs.release
        }
    }

    buildFeatures {
        // This is required for some Capacitor plugins
        buildConfig = true
    }
}

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])
    implementation "androidx.appcompat:appcompat:$androidxAppCompatVersion"
    implementation project(':capacitor-android')
    testImplementation "junit:junit:$junitVersion"
    androidTestImplementation "androidx.test.ext:junit:$androidxJunitVersion"
    androidTestImplementation "androidx.test.espresso:espresso-core:$androidxEspressoCoreVersion"
    implementation project(':capacitor-app')
    implementation project(':capacitor-camera')
    implementation project(':capacitor-community-bluetooth-le')
    implementation project(':capacitor-community-sqlite')
    implementation project(':capacitor-status-bar')
}

apply from: 'capacitor.build.gradle'
try {
    // This line applies the google-services plugin for Firebase
    apply plugin: 'com.google.gms.google-services'
} catch (Exception e) {
    logger.warn("google-services plugin not found, did you add it to your project root build.gradle?")
}
