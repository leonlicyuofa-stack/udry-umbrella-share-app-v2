ext {
    // This file is kept for compatibility with some Capacitor plugins.
    // However, we are moving towards defining versions in libs.versions.toml
    // and hardcoding SDK versions in build.gradle files where necessary.
}
